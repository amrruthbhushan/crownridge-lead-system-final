# SQLite database (file-based, no external DB needed)
DATABASE_URL="file:./prisma/dev.db"

# JWT Auth Secret - change this in production!
JWT_SECRET="crownridge-super-secret-security-key-2026"

# Port to run the backend server
PORT=5000

# Frontend URL (used for CORS in production)
# FRONTEND_URL=https://your-app-name.netlify.app
